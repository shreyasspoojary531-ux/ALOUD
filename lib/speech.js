export function say(text){if(typeof window==='undefined'||!('speechSynthesis'in window))return false;const talk=()=>{speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance(text))};talk();return true}
